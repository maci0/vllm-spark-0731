# Copy to nodes.env and fill QSFP IPs (not the LAN).
#   ip -4 addr show enp1s0f0np0
#
# On the head:  NODE_RANK=0  VLLM_HOST_IP=<this fabric IP>  HEAD_IP=<this fabric IP>
# On the worker: NODE_RANK=1 VLLM_HOST_IP=<this fabric IP> HEAD_IP=<head fabric IP>

HEAD_IP=192.168.1.10
# VLLM_HOST_IP=192.168.1.10
# NODE_RANK=0
